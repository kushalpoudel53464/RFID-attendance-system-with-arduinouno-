The above code use a excel sheet to use input the data form rfid and arduino . 
when you tap the rfid code in rfid module, it form a unique id where we can assume the value and replace the we wanted. 
